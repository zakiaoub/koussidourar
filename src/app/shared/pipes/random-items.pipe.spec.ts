import { RandomItemsPipe } from './random-items.pipe';

describe('RandomItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
